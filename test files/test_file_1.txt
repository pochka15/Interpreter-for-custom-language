#T
main () void {
    # constant
    let elements List = [1, 2, 3]

    print_at_even_pos(elements)
    
    let last int = elements[elements.size() - 1]
    print(elements.add(add_one(last)))
}

tmp () void {
    ret "hello"
}

add_one (element int) int {
     ret element + 1
}

print_at_even_pos (elements List) void {
    var i int = 0
    for val in elements {
        let is_even bool = i % 2 == 0
        print(if is_even { toString(val) } else { "" })
        i = i + 1
    }
}